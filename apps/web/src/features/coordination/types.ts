export type AssignmentTaskType = 'normal' | 'exploration';
export type Flow = {pending_action_count?:number;follow_up_ready?:boolean;id:string;owner_id:string;kind:'sync'|'decision'|'assignment'|'chat_summary'|'task_summary';title:string;status:string;member_ids:string[];thread_id:string;task_id:string;updated_at:string;task_type?:AssignmentTaskType;topic_id?:string};
export type FlowData = Flow & {body:string;error:string;sources_changed?:boolean;result:{summary?:string;candidates?:{person_id:string;reason:string}[]};evidence:{person_id:string;answer:string;sources:{id:string;title:string;source_kind:string;version:number}[]}[];actions:{id:string;assignee_id:string;title:string;detail:string;status:string;task_id:string;task_status?:string;task_artifact?:string}[];follow_up?:{ready:boolean;status:'waiting'|'suggested'|'created'|'dismissed';next_flow_id:string;completed_count:number;task_count:number}};
export const flowLabels:Record<string,string>={queued:'等待整理',running:'正在整理',summarizing:'正在整理纪要',ready:'待你选择',live:'会议中',closed:'已整理',assigned:'已分配',error:'需要重试'};
